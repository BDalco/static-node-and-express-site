extends layout.pug

block content

  article.portfolio-intro
    h1 My Portfolio
    p Enjoy my portfolio website which showcases my JavaScript skills with some examples of projects that I have worked on.

  article.portfolio-project-thumbs
    .projects-box.box.grid
      each project in projects
        .cell
          a.project(href=`/project/${project.id}`)
            img.thumbnail(src=`${project.gallery_urls[0]}`, alt=`${project.project_name}`)
            h5=project.project_name
